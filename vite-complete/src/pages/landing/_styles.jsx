import { styled } from "@mui/material/styles";

export const Container = styled("section")(({ theme }) => ({}));

export const ActionWraper = styled("div")(({ theme }) => ({
  display: "flex",
  alignItems: "center",
  gap: 10,
}));

export const Text = styled("h1")(({ theme }) => ({}));
export const Span = styled("span")(({ theme }) => ({}));
